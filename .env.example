# model-set Environment Variables
# Copy this file to .env and fill in your values

# =====================================================
# GLOBAL MCP SERVERS (always available)
# =====================================================

# Stitch - UI design generation
# NOTE: Stitch uses OAuth (no API key needed!)
# Setup: npx stitch-mcp-auto-setup (opens browser for Google auth)
# The setup wizard will configure your Google Cloud project automatically.
GOOGLE_CLOUD_PROJECT=your-gcloud-project-id

# Context7 - Documentation lookup
CONTEXT7_API_KEY=ctx7sk-your-api-key-here

# =====================================================
# CODEX CLI (OpenAI)
# =====================================================

# OpenAI API Key (optional - Codex can also use OAuth login via `codex login`)
# Required for headless/CI usage (e.g. Ralph agent)
# OPENAI_API_KEY=sk-your-openai-api-key-here

# =====================================================
# LOCAL MCP SERVERS (project-specific, set in project .env)
# =====================================================

# PostgreSQL database connection
# POSTGRES_DATABASE_URI=postgresql://user:password@host:5432/database

# Redis connection
# REDIS_URL=redis://:password@localhost:6379/0

# Stripe (if using)
# STRIPE_API_KEY=sk_test_your-stripe-key-here
