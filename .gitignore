# Secrets
.env
.env.local
.env.*.local
!.env.example

# Generated project MCP config (the .template source is tracked)
.mcp.json
!**/.mcp.json.template

# Credentials & auth tokens — belt and braces; under file ownership these no
# longer live inside the repo at all.
**/.credentials.json
**/auth.json
**/oauth_creds.json

# OS / editor / language
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
node_modules/
__pycache__/

# Agent file sync backups (auto-generated)
agent_file_backups/

# Codex writes its own system skills here at runtime — machine state, not ours
skills/.system/
